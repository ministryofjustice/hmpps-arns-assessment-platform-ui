import { step } from '@form-engine/form/builders'
import { pageContent } from './fields'

/**
 * Iterators Playground - Map Examples
 *
 * Interactive examples of Iterator.Map.
 */
export const mapExamplesStep = step({
  path: '/map-examples',
  title: 'Map Examples',
  blocks: [pageContent],
})
