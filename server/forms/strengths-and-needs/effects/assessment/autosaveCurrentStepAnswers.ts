/**
 * Autosave counterpart to saveCurrentStepAnswers,
 * kept separate so an autosave never touches the saved answers
 */
export const autosaveCurrentStepAnswers = () => async () => {
  // TODO: persist the step's answers to AAP once the backend supports it.
}
